// The deps-injectable Hono app (buildApp) lives in src/app.ts. The production
// entry point — wiring D1/R2/rate-limiter bindings into WorkerDeps (with
// instrumentD1/instrumentR2 usage tracking) — lands together with the content
// and audio routes in Phase 3.
export {};
